apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: cutomerrag
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: cutomerrag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
